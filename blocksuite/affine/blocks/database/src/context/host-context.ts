// Moved to `@blocksuite/data-view` core so `data-view`'s own view-preset
// renderers (e.g. list view's row-level actions, Story 2.6) can use the
// same identifier without a backwards dependency on this package.
export { EditorHostKey } from '@blocksuite/data-view';
